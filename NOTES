Create a global resource class that maps texture_path to Texture

Create new SpriteSheet class

Generate random fruits after last one is collected

Snake can collide with itself and end game
Fix snake eating fruit without being on tile

Add text rendering
Display score, increase whenever snake eats fruit
